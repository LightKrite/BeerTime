# BeerTime Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Веб-страница, которая показывает шести друзьям со сменным графиком 2/2, какие ближайшие вечера подходят всем.

**Architecture:** Вся доменная логика — чистые функции в `schedule.py` без внешних зависимостей: по циклу смен и правкам вычисляется род дня и статус вечера. `app.py` держит SQLite и маршруты FastAPI, рендерит матрицу «люди × даты» шаблонами Jinja2 и точечно обновляет клетки через HTMX.

**Tech Stack:** Python 3.11+, FastAPI, Jinja2, `sqlite3` из стандартной библиотеки, HTMX (один файл, положенный в `static/`), pytest.

## Global Constraints

- Спека: `docs/superpowers/specs/2026-08-27-beertime-design.md`. При расхождении спека главнее плана.
- Никакой ORM, никаких миграционных фреймворков — только модуль `sqlite3`.
- Никакого фронтенд-билда: ни npm, ни бандлера, ни `node_modules`. HTMX подключается локальным файлом из `static/`.
- Доменная логика живёт в `schedule.py` и не импортирует ни FastAPI, ни `sqlite3`. Это делает тесты логики мгновенными.
- Все даты внутри — `datetime.date`, в базе хранятся строками ISO (`YYYY-MM-DD`).
- Горизонт показа — 14 дней, начиная с сегодня. Константа `HORIZON_DAYS = 14` в `schedule.py`.
- Часовой пояс один на всех, из переменной окружения `TZ` (по умолчанию `Europe/Moscow`).
- Секретный сегмент пути — из переменной окружения `SECRET`. Приложение не стартует, если она не задана.
- Тексты интерфейса на русском.
- Коммиты после каждой задачи, сообщения на русском в формате `тип: описание`.

## File Structure

```
schedule.py          — чистая логика: Person, kind(), status(), day_summary(), rank(), next_override()
app.py               — SQLite (схема и запросы), маршруты FastAPI, рендеринг
templates/base.html  — каркас страницы, подключение HTMX и стилей
templates/index.html — матрица «люди × даты» + строка итогов
templates/cell.html  — одна клетка матрицы (партиал для HTMX-обновления)
templates/me.html    — форма настроек цикла
templates/join.html  — выбор себя из списка / добавление имени
static/htmx.min.js   — HTMX, скачанный локально
static/style.css     — стили матрицы
test_schedule.py     — тесты чистой логики
test_app.py          — тесты маршрутов и базы
requirements.txt
.env.example
deploy/beertime.service
deploy/nginx.conf
README.md
beertime.db          — база, в .gitignore
```

---

### Task 1: Скелет проекта

**Files:**
- Create: `requirements.txt`, `.env.example`, `README.md`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: ничего
- Produces: рабочее окружение, в котором запускается `pytest`

- [ ] **Step 1: Создать `requirements.txt`**

```
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
pytest==8.3.4
httpx==0.28.1
```

- [ ] **Step 2: Создать `.env.example`**

```
# Секретный сегмент пути: страница живёт на /b/<SECRET>/
SECRET=changeme-длинная-случайная-строка
# Часовой пояс компании
TZ=Europe/Moscow
# Файл базы
DB_PATH=beertime.db
```

- [ ] **Step 3: Дописать `.gitignore`**

Добавить в конец файла:

```
beertime.db
__pycache__/
*.pyc
.pytest_cache/
```

- [ ] **Step 4: Создать `README.md`**

```markdown
# BeerTime

Находит вечера, когда вся компания может собраться, исходя из сменных графиков 2/2.

## Запуск

    python -m venv .venv && source .venv/bin/activate
    pip install -r requirements.txt
    cp .env.example .env   # и вписать свой SECRET
    set -a && source .env && set +a
    uvicorn app:app --reload

Страница открывается на `http://localhost:8000/b/<SECRET>/`.

## Тесты

    pytest -q
```

- [ ] **Step 5: Проверить, что окружение ставится**

Run: `python -m venv .venv && .venv/bin/pip install -q -r requirements.txt && .venv/bin/pytest --version`
Expected: печатается версия pytest, ошибок установки нет.

- [ ] **Step 6: Коммит**

```bash
git add requirements.txt .env.example README.md .gitignore
git commit -m "chore: скелет проекта и зависимости"
```

---

### Task 2: Род дня — `kind()`

Вычисляет, что у человека в конкретный день: выходной, дневная смена, ночная смена или «занят». Правки перекрывают вычисленный паттерн.

**Files:**
- Create: `schedule.py`
- Test: `test_schedule.py`

**Interfaces:**
- Consumes: ничего
- Produces:
  - константы `OFF = "off"`, `DAY = "day"`, `NIGHT = "night"`, `BUSY = "busy"`, `ALTERNATE = "alternate"`
  - `HORIZON_DAYS = 14`
  - `class Person(NamedTuple)` с полями `id: int`, `name: str`, `cycle_on: int`, `cycle_off: int`, `anchor: date`, `mode: str`
  - `kind(person: Person, d: date, overrides: dict[date, str]) -> str`

- [ ] **Step 1: Написать падающий тест**

Создать `test_schedule.py`:

```python
from datetime import date

from schedule import ALTERNATE, BUSY, DAY, NIGHT, OFF, Person, kind

ANCHOR = date(2026, 8, 12)  # среда, первая смена цикла


def p(**kw):
    base = dict(id=1, name="Егор", cycle_on=2, cycle_off=2, anchor=ANCHOR, mode=DAY)
    return Person(**{**base, **kw})


def test_cycle_2_2_дневной_режим():
    person = p()
    assert kind(person, date(2026, 8, 12), {}) == DAY    # 1-я смена
    assert kind(person, date(2026, 8, 13), {}) == DAY    # 2-я смена
    assert kind(person, date(2026, 8, 14), {}) == OFF    # выходной
    assert kind(person, date(2026, 8, 15), {}) == OFF    # выходной
    assert kind(person, date(2026, 8, 16), {}) == DAY    # цикл пошёл заново


def test_ночной_режим_всегда_ночь():
    person = p(mode=NIGHT)
    assert kind(person, date(2026, 8, 12), {}) == NIGHT
    assert kind(person, date(2026, 8, 14), {}) == OFF


def test_чередование_день_ночь_по_блокам():
    person = p(mode=ALTERNATE)
    assert kind(person, date(2026, 8, 12), {}) == DAY     # блок 0
    assert kind(person, date(2026, 8, 13), {}) == DAY
    assert kind(person, date(2026, 8, 16), {}) == NIGHT   # блок 1
    assert kind(person, date(2026, 8, 17), {}) == NIGHT
    assert kind(person, date(2026, 8, 20), {}) == DAY     # блок 2


def test_даты_раньше_якоря_разворачиваются_назад():
    person = p(mode=ALTERNATE)
    assert kind(person, date(2026, 8, 11), {}) == OFF     # выходной перед якорем
    assert kind(person, date(2026, 8, 10), {}) == OFF
    assert kind(person, date(2026, 8, 9), {}) == NIGHT    # предыдущий блок
    assert kind(person, date(2026, 8, 8), {}) == NIGHT


def test_нечётный_цикл_3_3():
    person = p(cycle_on=3, cycle_off=3)
    assert kind(person, date(2026, 8, 14), {}) == DAY     # 3-я смена
    assert kind(person, date(2026, 8, 15), {}) == OFF
    assert kind(person, date(2026, 8, 18), {}) == DAY     # цикл заново


def test_правка_перекрывает_паттерн():
    person = p()
    overrides = {date(2026, 8, 12): OFF, date(2026, 8, 14): BUSY}
    assert kind(person, date(2026, 8, 12), overrides) == OFF
    assert kind(person, date(2026, 8, 14), overrides) == BUSY
    assert kind(person, date(2026, 8, 13), overrides) == DAY  # соседние дни не тронуты
```

- [ ] **Step 2: Запустить тест, убедиться что падает**

Run: `.venv/bin/pytest test_schedule.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'schedule'`

- [ ] **Step 3: Написать минимальную реализацию**

Создать `schedule.py`:

```python
"""Чистая логика сменных графиков. Без зависимостей и без ввода-вывода."""

from __future__ import annotations

from datetime import date
from typing import NamedTuple

OFF = "off"
DAY = "day"
NIGHT = "night"
BUSY = "busy"

ALTERNATE = "alternate"

HORIZON_DAYS = 14


class Person(NamedTuple):
    id: int
    name: str
    cycle_on: int
    cycle_off: int
    anchor: date
    mode: str  # DAY | NIGHT | ALTERNATE


def kind(person: Person, d: date, overrides: dict[date, str]) -> str:
    """Род дня d: OFF | DAY | NIGHT | BUSY.

    Правка на конкретную дату перекрывает вычисленный паттерн.
    Деление в Python floor-based, поэтому даты раньше якоря разворачиваются
    назад сами: остаток неотрицателен, а номер блока уходит в минус.
    """
    if d in overrides:
        return overrides[d]
    period = person.cycle_on + person.cycle_off
    n = (d - person.anchor).days
    if n % period >= person.cycle_on:
        return OFF
    if person.mode != ALTERNATE:
        return person.mode
    return DAY if (n // period) % 2 == 0 else NIGHT
```

- [ ] **Step 4: Запустить тесты, убедиться что проходят**

Run: `.venv/bin/pytest test_schedule.py -q`
Expected: PASS, 6 тестов.

- [ ] **Step 5: Коммит**

```bash
git add schedule.py test_schedule.py
git commit -m "feat: вычисление рода дня по сменному циклу"
```

---

### Task 3: Статус вечера — `status()`

**Files:**
- Modify: `schedule.py`
- Test: `test_schedule.py`

**Interfaces:**
- Consumes: `Person`, `kind()`, константы из Task 2
- Produces:
  - константы `GREEN = "green"`, `YELLOW = "yellow"`, `RED = "red"`, `BLOCKED = "blocked"`
  - `status(person: Person, d: date, overrides: dict[date, str]) -> str`

- [ ] **Step 1: Написать падающий тест**

Дописать в `test_schedule.py`:

```python
from datetime import timedelta

from schedule import BLOCKED, GREEN, RED, YELLOW, status


def test_ночная_смена_сегодня_это_красный():
    person = p(mode=NIGHT)
    assert status(person, date(2026, 8, 12), {}) == RED


def test_завтра_дневная_смена_это_жёлтый():
    person = p()
    # 15-е — выходной, 16-е — дневная смена
    assert status(person, date(2026, 8, 15), {}) == YELLOW


def test_завтра_ночная_смена_это_зелёный():
    person = p(mode=NIGHT)
    # 15-е — выходной, 16-е — ночная смена: завтра спит днём, вечер свободен
    assert status(person, date(2026, 8, 15), {}) == GREEN


def test_после_дневной_смены_перед_выходным_зелёный():
    person = p()
    # 13-е — вторая дневная смена, 14-е — выходной
    assert status(person, date(2026, 8, 13), {}) == GREEN


def test_дневная_смена_и_завтра_дневная_это_жёлтый():
    person = p()
    assert status(person, date(2026, 8, 12), {}) == YELLOW


def test_два_выходных_подряд_зелёный():
    person = p()
    assert status(person, date(2026, 8, 14), {}) == GREEN


def test_занят_сегодня_блокирует_вечер():
    person = p()
    assert status(person, date(2026, 8, 14), {date(2026, 8, 14): BUSY}) == BLOCKED


def test_занят_завтра_не_даёт_жёлтый_сегодня():
    person = p()
    # 16-е было бы дневной сменой и дало бы жёлтый на 15-е,
    # но "занят" — это не работа, раннего подъёма из него не следует
    assert status(person, date(2026, 8, 15), {date(2026, 8, 16): BUSY}) == GREEN
```

- [ ] **Step 2: Запустить тест, убедиться что падает**

Run: `.venv/bin/pytest test_schedule.py -q`
Expected: FAIL — `ImportError: cannot import name 'GREEN' from 'schedule'`

- [ ] **Step 3: Написать минимальную реализацию**

Дописать в `schedule.py` (импорт `timedelta` добавить к существующему импорту `date`):

```python
GREEN = "green"    # гуляем
YELLOW = "yellow"  # можно, но завтра рано вставать
RED = "red"        # вечером на смену
BLOCKED = "blocked"  # свои дела


def status(person: Person, d: date, overrides: dict[date, str]) -> str:
    """Можно ли человеку пить вечером дня d."""
    today = kind(person, d, overrides)
    if today == BUSY:
        return BLOCKED
    if today == NIGHT:
        return RED
    if kind(person, d + timedelta(days=1), overrides) == DAY:
        return YELLOW
    return GREEN
```

- [ ] **Step 4: Запустить тесты, убедиться что проходят**

Run: `.venv/bin/pytest test_schedule.py -q`
Expected: PASS, 14 тестов.

- [ ] **Step 5: Коммит**

```bash
git add schedule.py test_schedule.py
git commit -m "feat: статус вечера по роду сегодняшнего и завтрашнего дня"
```

---

### Task 4: Итоги по дню, рейтинг вечеров и цикл правки

**Files:**
- Modify: `schedule.py`
- Test: `test_schedule.py`

**Interfaces:**
- Consumes: константы статусов из Task 3
- Produces:
  - `day_summary(statuses: list[str]) -> tuple[int, int, int]` — сколько зелёных, жёлтых, блокирующих (`RED` и `BLOCKED` вместе)
  - `rank(days: list[tuple[date, tuple[int, int, int]]]) -> list[tuple[date, tuple[int, int, int]]]` — те же дни, отсортированные от лучшего к худшему
  - `OVERRIDE_CYCLE: tuple[str | None, ...]` и `next_override(current: str | None) -> str | None`

- [ ] **Step 1: Написать падающий тест**

Дописать в `test_schedule.py`:

```python
from schedule import day_summary, next_override, rank


def test_итог_по_дню_считает_три_числа():
    statuses = [GREEN, GREEN, YELLOW, RED, BLOCKED, GREEN]
    assert day_summary(statuses) == (3, 1, 2)


def test_итог_по_пустому_дню():
    assert day_summary([]) == (0, 0, 0)


def test_рейтинг_ставит_дни_без_блокирующих_выше():
    days = [
        (date(2026, 8, 20), (5, 1, 0)),
        (date(2026, 8, 18), (6, 0, 1)),
        (date(2026, 8, 19), (4, 2, 0)),
    ]
    assert [d for d, _ in rank(days)] == [
        date(2026, 8, 20),  # ноль блокирующих, 5 зелёных
        date(2026, 8, 19),  # ноль блокирующих, 4 зелёных
        date(2026, 8, 18),  # есть блокирующий — вниз, несмотря на 6 зелёных
    ]


def test_при_равных_счётчиках_выигрывает_ближняя_дата():
    days = [(date(2026, 8, 25), (4, 1, 0)), (date(2026, 8, 19), (4, 1, 0))]
    assert [d for d, _ in rank(days)] == [date(2026, 8, 19), date(2026, 8, 25)]


def test_цикл_правки_дня():
    assert next_override(None) == OFF
    assert next_override(OFF) == DAY
    assert next_override(DAY) == NIGHT
    assert next_override(NIGHT) == BUSY
    assert next_override(BUSY) is None  # сброс к паттерну
```

- [ ] **Step 2: Запустить тест, убедиться что падает**

Run: `.venv/bin/pytest test_schedule.py -q`
Expected: FAIL — `ImportError: cannot import name 'day_summary' from 'schedule'`

- [ ] **Step 3: Написать минимальную реализацию**

Дописать в `schedule.py`:

```python
OVERRIDE_CYCLE: tuple[str | None, ...] = (None, OFF, DAY, NIGHT, BUSY)


def day_summary(statuses: list[str]) -> tuple[int, int, int]:
    """Сколько в этот вечер зелёных, жёлтых и блокирующих (RED и BLOCKED)."""
    return (
        statuses.count(GREEN),
        statuses.count(YELLOW),
        sum(s in (RED, BLOCKED) for s in statuses),
    )


def rank(days: list[tuple[date, tuple[int, int, int]]]) -> list[tuple[date, tuple[int, int, int]]]:
    """Лучшие вечера вперёд: сначала без блокирующих, затем больше зелёных, затем ближе дата."""
    return sorted(days, key=lambda item: (item[1][2], -item[1][0], item[0]))


def next_override(current: str | None) -> str | None:
    """Следующее значение по кругу: паттерн → выходной → день → ночь → занят → паттерн."""
    return OVERRIDE_CYCLE[(OVERRIDE_CYCLE.index(current) + 1) % len(OVERRIDE_CYCLE)]
```

- [ ] **Step 4: Запустить тесты, убедиться что проходят**

Run: `.venv/bin/pytest test_schedule.py -q`
Expected: PASS, 19 тестов.

- [ ] **Step 5: Коммит**

```bash
git add schedule.py test_schedule.py
git commit -m "feat: итоги по дню, рейтинг вечеров и цикл правки"
```

---

### Task 5: База и настройки приложения

**Files:**
- Create: `app.py`
- Test: `test_app.py`

**Interfaces:**
- Consumes: `Person`, `DAY`, `ALTERNATE` из `schedule.py`
- Produces:
  - `DB_PATH`, `SECRET`, `TZ` — читаются из окружения при импорте модуля; отсутствие `SECRET` роняет импорт с `RuntimeError`
  - `connect() -> sqlite3.Connection` — соединение с включённым `row_factory`
  - `init_db() -> None` — создаёт таблицы, если их нет
  - `today() -> date` — сегодняшняя дата в часовом поясе компании
  - `list_people() -> list[Person]`
  - `add_person(name: str) -> int` — возвращает id
  - `update_person(person_id: int, cycle_on: int, cycle_off: int, anchor: date, mode: str) -> None`
  - `overrides_for(person_id: int) -> dict[date, str]`
  - `set_override(person_id: int, d: date, value: str | None) -> None` — `None` удаляет правку

- [ ] **Step 1: Написать падающий тест**

Создать `test_app.py`:

```python
import os
from datetime import date

os.environ.setdefault("SECRET", "test-secret")
os.environ.setdefault("TZ", "Europe/Moscow")

import pytest

import app as app_module
from schedule import ALTERNATE, BUSY, DAY, OFF


@pytest.fixture(autouse=True)
def temp_db(tmp_path, monkeypatch):
    monkeypatch.setattr(app_module, "DB_PATH", str(tmp_path / "test.db"))
    app_module.init_db()
    yield


def test_добавить_и_прочитать_человека():
    person_id = app_module.add_person("Егор")
    people = app_module.list_people()
    assert [p.name for p in people] == ["Егор"]
    assert people[0].id == person_id
    assert people[0].cycle_on == 2 and people[0].cycle_off == 2
    assert people[0].mode == DAY


def test_обновить_настройки_цикла():
    person_id = app_module.add_person("Макс")
    app_module.update_person(person_id, 3, 3, date(2026, 8, 12), ALTERNATE)
    person = app_module.list_people()[0]
    assert (person.cycle_on, person.cycle_off, person.mode) == (3, 3, ALTERNATE)
    assert person.anchor == date(2026, 8, 12)


def test_правки_записываются_перезаписываются_и_удаляются():
    person_id = app_module.add_person("Ден")
    d = date(2026, 8, 20)
    app_module.set_override(person_id, d, OFF)
    assert app_module.overrides_for(person_id) == {d: OFF}
    app_module.set_override(person_id, d, BUSY)
    assert app_module.overrides_for(person_id) == {d: BUSY}
    app_module.set_override(person_id, d, None)
    assert app_module.overrides_for(person_id) == {}


def test_правки_не_протекают_между_людьми():
    a = app_module.add_person("Саня")
    b = app_module.add_person("Витя")
    app_module.set_override(a, date(2026, 8, 20), BUSY)
    assert app_module.overrides_for(b) == {}
```

- [ ] **Step 2: Запустить тест, убедиться что падает**

Run: `.venv/bin/pytest test_app.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'app'`

- [ ] **Step 3: Написать минимальную реализацию**

Создать `app.py`:

```python
"""BeerTime: база, маршруты и рендеринг."""

from __future__ import annotations

import os
import sqlite3
from datetime import date, datetime
from zoneinfo import ZoneInfo

from schedule import ALTERNATE, DAY, Person

SECRET = os.environ.get("SECRET")
if not SECRET:
    raise RuntimeError("Не задана переменная окружения SECRET")

TZ = ZoneInfo(os.environ.get("TZ", "Europe/Moscow"))
DB_PATH = os.environ.get("DB_PATH", "beertime.db")

SCHEMA = """
CREATE TABLE IF NOT EXISTS person (
    id          INTEGER PRIMARY KEY,
    name        TEXT NOT NULL,
    cycle_on    INTEGER NOT NULL DEFAULT 2,
    cycle_off   INTEGER NOT NULL DEFAULT 2,
    anchor_date TEXT NOT NULL,
    mode        TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS override (
    person_id INTEGER NOT NULL REFERENCES person(id),
    date      TEXT NOT NULL,
    kind      TEXT NOT NULL,
    PRIMARY KEY (person_id, date)
);
"""


def connect() -> sqlite3.Connection:
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row
    return conn


def init_db() -> None:
    with connect() as conn:
        conn.executescript(SCHEMA)


def today() -> date:
    return datetime.now(TZ).date()


def list_people() -> list[Person]:
    with connect() as conn:
        rows = conn.execute("SELECT * FROM person ORDER BY id").fetchall()
    return [
        Person(
            id=row["id"],
            name=row["name"],
            cycle_on=row["cycle_on"],
            cycle_off=row["cycle_off"],
            anchor=date.fromisoformat(row["anchor_date"]),
            mode=row["mode"],
        )
        for row in rows
    ]


def add_person(name: str) -> int:
    with connect() as conn:
        cur = conn.execute(
            "INSERT INTO person (name, anchor_date, mode) VALUES (?, ?, ?)",
            (name, today().isoformat(), DAY),
        )
        return cur.lastrowid


def update_person(person_id: int, cycle_on: int, cycle_off: int, anchor: date, mode: str) -> None:
    with connect() as conn:
        conn.execute(
            "UPDATE person SET cycle_on = ?, cycle_off = ?, anchor_date = ?, mode = ? WHERE id = ?",
            (cycle_on, cycle_off, anchor.isoformat(), mode, person_id),
        )


def overrides_for(person_id: int) -> dict[date, str]:
    with connect() as conn:
        rows = conn.execute(
            "SELECT date, kind FROM override WHERE person_id = ?", (person_id,)
        ).fetchall()
    return {date.fromisoformat(row["date"]): row["kind"] for row in rows}


def set_override(person_id: int, d: date, value: str | None) -> None:
    with connect() as conn:
        if value is None:
            conn.execute(
                "DELETE FROM override WHERE person_id = ? AND date = ?",
                (person_id, d.isoformat()),
            )
        else:
            conn.execute(
                "INSERT INTO override (person_id, date, kind) VALUES (?, ?, ?) "
                "ON CONFLICT (person_id, date) DO UPDATE SET kind = excluded.kind",
                (person_id, d.isoformat(), value),
            )
```

- [ ] **Step 4: Запустить тесты, убедиться что проходят**

Run: `.venv/bin/pytest -q`
Expected: PASS, 23 теста.

- [ ] **Step 5: Коммит**

```bash
git add app.py test_app.py
git commit -m "feat: схема SQLite и запросы к людям и правкам"
```

---

### Task 6: Сборка матрицы

Чистая сборка данных для страницы: строки-люди, колонки-даты, итоги и лучшие вечера. Отдельная функция, чтобы её можно было проверить без HTTP.

**Files:**
- Modify: `app.py`
- Test: `test_app.py`

**Interfaces:**
- Consumes: `list_people()`, `overrides_for()`, `today()`, `status()`, `day_summary()`, `rank()`, `HORIZON_DAYS`
- Produces: `build_matrix() -> dict` со структурой:
  ```python
  {
      "dates": [date, ...],                       # HORIZON_DAYS дней, начиная с сегодня
      "rows": [{"person": Person, "statuses": [str, ...]}, ...],
      "summaries": [(green, yellow, blocked), ...],  # по одной на дату
      "best": [date, ...],                        # до трёх лучших вечеров, лучший первым
  }
  ```

- [ ] **Step 1: Написать падающий тест**

Дописать в `test_app.py`:

```python
from datetime import timedelta

from schedule import GREEN, HORIZON_DAYS, NIGHT


def test_матрица_имеет_строку_на_человека_и_колонку_на_дату():
    app_module.add_person("Егор")
    app_module.add_person("Макс")
    matrix = app_module.build_matrix()
    assert len(matrix["dates"]) == HORIZON_DAYS
    assert matrix["dates"][0] == app_module.today()
    assert [row["person"].name for row in matrix["rows"]] == ["Егор", "Макс"]
    assert all(len(row["statuses"]) == HORIZON_DAYS for row in matrix["rows"])
    assert len(matrix["summaries"]) == HORIZON_DAYS


def test_итоги_совпадают_со_статусами_колонки():
    app_module.add_person("Егор")
    app_module.add_person("Макс")
    matrix = app_module.build_matrix()
    for i, summary in enumerate(matrix["summaries"]):
        column = [row["statuses"][i] for row in matrix["rows"]]
        assert summary == app_module.day_summary(column)


def test_лучший_вечер_первым_в_списке_best():
    person_id = app_module.add_person("Егор")
    # всё занято, кроме послезавтра — оно и должно оказаться лучшим
    for offset in range(HORIZON_DAYS):
        if offset != 2:
            app_module.set_override(person_id, app_module.today() + timedelta(days=offset), BUSY)
    matrix = app_module.build_matrix()
    assert matrix["best"][0] == app_module.today() + timedelta(days=2)


def test_матрица_без_людей_не_падает():
    matrix = app_module.build_matrix()
    assert matrix["rows"] == []
    assert matrix["summaries"] == [(0, 0, 0)] * HORIZON_DAYS
```

- [ ] **Step 2: Запустить тест, убедиться что падает**

Run: `.venv/bin/pytest test_app.py -q`
Expected: FAIL — `AttributeError: module 'app' has no attribute 'build_matrix'`

- [ ] **Step 3: Написать минимальную реализацию**

Дописать в `app.py` (расширив импорт из `schedule`):

```python
from datetime import timedelta

from schedule import HORIZON_DAYS, day_summary, rank, status


def build_matrix() -> dict:
    """Данные страницы: строки-люди, колонки-даты, итоги и лучшие вечера."""
    start = today()
    dates = [start + timedelta(days=i) for i in range(HORIZON_DAYS)]
    people = list_people()

    rows = []
    for person in people:
        overrides = overrides_for(person.id)
        rows.append(
            {
                "person": person,
                "statuses": [status(person, d, overrides) for d in dates],
                "overrides": overrides,
            }
        )

    summaries = [
        day_summary([row["statuses"][i] for row in rows]) for i in range(len(dates))
    ]
    best = [d for d, _ in rank(list(zip(dates, summaries)))[:3]]
    return {"dates": dates, "rows": rows, "summaries": summaries, "best": best}
```

Также добавить реэкспорт `day_summary` — он уже импортирован в модуль, тест обращается к нему как `app_module.day_summary`.

- [ ] **Step 4: Запустить тесты, убедиться что проходят**

Run: `.venv/bin/pytest -q`
Expected: PASS, 27 тестов.

- [ ] **Step 5: Коммит**

```bash
git add app.py test_app.py
git commit -m "feat: сборка матрицы доступности и рейтинга вечеров"
```

---

### Task 7: Маршруты — секретный путь, вход, матрица

**Files:**
- Modify: `app.py`
- Create: `templates/base.html`, `templates/index.html`, `templates/cell.html`, `templates/join.html`, `static/style.css`, `static/htmx.min.js`
- Test: `test_app.py`

**Interfaces:**
- Consumes: `build_matrix()`, `list_people()`, `add_person()`, `SECRET`
- Produces:
  - `app: FastAPI`
  - `GET /b/{secret}/` — матрица, либо редирект на `/b/{secret}/join`, если куки `bt_person` нет
  - `GET /b/{secret}/join` — форма выбора себя
  - `POST /b/{secret}/join` — поля формы `person_id` (пустое, если новый) и `name`; ставит куку `bt_person` и редиректит на матрицу
  - неверный секрет в любом маршруте — ответ 404

- [ ] **Step 1: Написать падающий тест**

Дописать в `test_app.py`:

```python
from fastapi.testclient import TestClient


def client():
    return TestClient(app_module.app)


def test_неверный_секрет_даёт_404():
    assert client().get("/b/wrong-secret/").status_code == 404


def test_без_куки_редирект_на_вход():
    response = client().get("/b/test-secret/", follow_redirects=False)
    assert response.status_code == 303
    assert response.headers["location"] == "/b/test-secret/join"


def test_вход_новым_именем_ставит_куку():
    c = client()
    response = c.post("/b/test-secret/join", data={"person_id": "", "name": "Егор"}, follow_redirects=False)
    assert response.status_code == 303
    assert response.headers["location"] == "/b/test-secret/"
    assert c.cookies["bt_person"] == str(app_module.list_people()[0].id)


def test_вход_выбором_существующего_человека():
    person_id = app_module.add_person("Макс")
    c = client()
    c.post("/b/test-secret/join", data={"person_id": str(person_id), "name": ""}, follow_redirects=False)
    assert c.cookies["bt_person"] == str(person_id)
    assert len(app_module.list_people()) == 1  # дубликат не создан


def test_матрица_показывает_имена_и_даты():
    person_id = app_module.add_person("Егор")
    c = client()
    c.cookies.set("bt_person", str(person_id))
    body = c.get("/b/test-secret/").text
    assert "Егор" in body
    assert app_module.today().strftime("%d") in body
```

- [ ] **Step 2: Запустить тест, убедиться что падает**

Run: `.venv/bin/pytest test_app.py -q`
Expected: FAIL — `AttributeError: module 'app' has no attribute 'app'`

- [ ] **Step 3: Скачать HTMX локально**

Run: `curl -sL https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js -o static/htmx.min.js` (папку `static/` создать заранее)
Expected: файл примерно 50 КБ, `head -c 40 static/htmx.min.js` показывает начало минифицированного JS.

- [ ] **Step 4: Написать шаблоны**

`templates/base.html`:

```html
<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>BeerTime</title>
  <link rel="stylesheet" href="/b/{{ secret }}/static/style.css">
  <script src="/b/{{ secret }}/static/htmx.min.js"></script>
</head>
<body>
  <header><a href="/b/{{ secret }}/">BeerTime</a> · <a href="/b/{{ secret }}/me">мой график</a></header>
  {% block content %}{% endblock %}
</body>
</html>
```

`templates/cell.html` — одна клетка, она же цель HTMX-обмена:

```html
{% set editable = row.person.id == me_id %}
<td id="cell-{{ row.person.id }}-{{ d.isoformat() }}"
    class="cell {{ st }}{% if editable %} editable{% endif %}"
    {% if editable %}
    hx-post="/b/{{ secret }}/cell/{{ d.isoformat() }}"
    hx-target="this"
    hx-swap="outerHTML"
    {% endif %}>
  {{ {"green": "🟢", "yellow": "🟡", "red": "🔴", "blocked": "⛔"}[st] }}
</td>
```

`templates/index.html`:

```html
{% extends "base.html" %}
{% block content %}
<p class="best">
  Ближайшие лучшие вечера:
  {% for d in matrix.best %}<b>{{ d.strftime("%d.%m") }}</b>{% if not loop.last %}, {% endif %}{% endfor %}
</p>

<div class="scroll">
<table class="matrix">
  <thead>
    <tr>
      <th class="sticky">кто</th>
      {% for d in matrix.dates %}
      <th class="{% if d in matrix.best %}best-col{% endif %}">
        {{ ["Пн","Вт","Ср","Чт","Пт","Сб","Вс"][d.weekday()] }}<br>{{ d.strftime("%d.%m") }}
      </th>
      {% endfor %}
    </tr>
  </thead>
  <tbody>
    {% for row in matrix.rows %}
    <tr>
      <th class="sticky">{{ row.person.name }}</th>
      {% for d in matrix.dates %}
        {% with st = row.statuses[loop.index0] %}
          {% include "cell.html" %}
        {% endwith %}
      {% endfor %}
    </tr>
    {% endfor %}
  </tbody>
  <tfoot>
    <tr>
      <th class="sticky">итог</th>
      {% for s in matrix.summaries %}
      <td class="summary">{{ s[0] }}/{{ s[1] }}/{{ s[2] }}</td>
      {% endfor %}
    </tr>
  </tfoot>
</table>
</div>
<p class="hint">Тык по своей клетке: выходной → день → ночь → занят → обратно к графику.</p>
{% endblock %}
```

`templates/join.html`:

```html
{% extends "base.html" %}
{% block content %}
<h1>Кто ты?</h1>
<form method="post" action="/b/{{ secret }}/join">
  {% if people %}
  <select name="person_id">
    <option value="">— я новенький —</option>
    {% for p in people %}<option value="{{ p.id }}">{{ p.name }}</option>{% endfor %}
  </select>
  {% else %}
  <input type="hidden" name="person_id" value="">
  {% endif %}
  <input name="name" placeholder="Имя, если новенький">
  <button type="submit">Войти</button>
</form>
{% endblock %}
```

`static/style.css`:

```css
body { font-family: system-ui, sans-serif; margin: 1rem; }
.scroll { overflow-x: auto; }
.matrix { border-collapse: collapse; }
.matrix th, .matrix td { border: 1px solid #ddd; padding: .4rem .5rem; text-align: center; }
.sticky { position: sticky; left: 0; background: #fff; text-align: left; white-space: nowrap; }
.best-col { background: #eaf7ea; }
.cell.editable { cursor: pointer; }
.summary { font-size: .8rem; color: #666; white-space: nowrap; }
.hint { color: #666; font-size: .9rem; }
```

- [ ] **Step 5: Написать маршруты**

Дописать в `app.py`:

```python
from fastapi import FastAPI, Form, HTTPException, Request
from fastapi.responses import RedirectResponse
from fastapi.staticfiles import StaticFiles
from fastapi.templating import Jinja2Templates

app = FastAPI()
app.mount(f"/b/{SECRET}/static", StaticFiles(directory="static"), name="static")
templates = Jinja2Templates(directory="templates")

init_db()


def check_secret(secret: str) -> None:
    if secret != SECRET:
        raise HTTPException(status_code=404)


def me_id(request: Request) -> int | None:
    raw = request.cookies.get("bt_person")
    return int(raw) if raw and raw.isdigit() else None


@app.get("/b/{secret}/")
def index(request: Request, secret: str):
    check_secret(secret)
    if me_id(request) is None:
        return RedirectResponse(f"/b/{secret}/join", status_code=303)
    return templates.TemplateResponse(
        request,
        "index.html",
        {"secret": secret, "matrix": build_matrix(), "me_id": me_id(request)},
    )


@app.get("/b/{secret}/join")
def join_form(request: Request, secret: str):
    check_secret(secret)
    return templates.TemplateResponse(
        request, "join.html", {"secret": secret, "people": list_people()}
    )


@app.post("/b/{secret}/join")
def join(secret: str, person_id: str = Form(""), name: str = Form("")):
    check_secret(secret)
    if person_id.isdigit():
        chosen = int(person_id)
    elif name.strip():
        chosen = add_person(name.strip())
    else:
        return RedirectResponse(f"/b/{secret}/join", status_code=303)
    response = RedirectResponse(f"/b/{secret}/", status_code=303)
    response.set_cookie("bt_person", str(chosen), max_age=60 * 60 * 24 * 365, httponly=True)
    return response
```

- [ ] **Step 6: Запустить тесты, убедиться что проходят**

Run: `.venv/bin/pytest -q`
Expected: PASS, 32 теста.

- [ ] **Step 7: Коммит**

```bash
git add app.py templates static test_app.py
git commit -m "feat: матрица на странице, вход по имени, секретный путь"
```

---

### Task 8: Правка своей клетки через HTMX

**Files:**
- Modify: `app.py`
- Test: `test_app.py`

**Interfaces:**
- Consumes: `next_override()`, `set_override()`, `overrides_for()`, `status()`, шаблон `cell.html`
- Produces: `POST /b/{secret}/cell/{iso_date}` — крутит правку своего дня по кругу и возвращает HTML одной клетки

- [ ] **Step 1: Написать падающий тест**

Дописать в `test_app.py`:

```python
from schedule import NIGHT


def test_тык_по_клетке_крутит_правку_по_кругу():
    person_id = app_module.add_person("Егор")
    d = app_module.today() + timedelta(days=1)
    c = client()
    c.cookies.set("bt_person", str(person_id))
    url = f"/b/test-secret/cell/{d.isoformat()}"

    c.post(url)
    assert app_module.overrides_for(person_id) == {d: OFF}
    c.post(url)
    assert app_module.overrides_for(person_id) == {d: DAY}
    c.post(url)
    assert app_module.overrides_for(person_id) == {d: NIGHT}
    c.post(url)
    assert app_module.overrides_for(person_id) == {d: BUSY}
    c.post(url)
    assert app_module.overrides_for(person_id) == {}  # сброс к графику


def test_тык_возвращает_html_одной_клетки():
    person_id = app_module.add_person("Егор")
    d = app_module.today() + timedelta(days=1)
    c = client()
    c.cookies.set("bt_person", str(person_id))
    body = c.post(f"/b/test-secret/cell/{d.isoformat()}").text
    assert body.strip().startswith("<td")
    assert f"cell-{person_id}-{d.isoformat()}" in body


def test_без_куки_править_нельзя():
    app_module.add_person("Егор")
    d = app_module.today().isoformat()
    assert client().post(f"/b/test-secret/cell/{d}").status_code == 403
```

- [ ] **Step 2: Запустить тест, убедиться что падает**

Run: `.venv/bin/pytest test_app.py -q`
Expected: FAIL — 405 или 404 вместо ожидаемого поведения: маршрута нет.

- [ ] **Step 3: Написать минимальную реализацию**

Дописать в `app.py` (расширив импорт из `schedule` на `next_override`):

```python
@app.post("/b/{secret}/cell/{iso_date}")
def toggle_cell(request: Request, secret: str, iso_date: str):
    check_secret(secret)
    person_id = me_id(request)
    if person_id is None:
        raise HTTPException(status_code=403)

    d = date.fromisoformat(iso_date)
    overrides = overrides_for(person_id)
    set_override(person_id, d, next_override(overrides.get(d)))

    person = next(p for p in list_people() if p.id == person_id)
    overrides = overrides_for(person_id)
    return templates.TemplateResponse(
        request,
        "cell.html",
        {
            "secret": secret,
            "row": {"person": person},
            "d": d,
            "st": status(person, d, overrides),
            "me_id": person_id,
        },
    )
```

- [ ] **Step 4: Запустить тесты, убедиться что проходят**

Run: `.venv/bin/pytest -q`
Expected: PASS, 35 тестов.

- [ ] **Step 5: Коммит**

```bash
git add app.py test_app.py
git commit -m "feat: правка своего дня тыком по клетке"
```

---

### Task 9: Страница настроек графика

**Files:**
- Modify: `app.py`
- Create: `templates/me.html`
- Test: `test_app.py`

**Interfaces:**
- Consumes: `update_person()`, `list_people()`, `me_id()`
- Produces:
  - `GET /b/{secret}/me` — форма с текущими настройками
  - `POST /b/{secret}/me` — поля `cycle_on`, `cycle_off`, `anchor`, `mode`; сохраняет и редиректит на матрицу

- [ ] **Step 1: Написать падающий тест**

Дописать в `test_app.py`:

```python
def test_форма_настроек_показывает_текущие_значения():
    person_id = app_module.add_person("Егор")
    app_module.update_person(person_id, 3, 3, date(2026, 8, 12), ALTERNATE)
    c = client()
    c.cookies.set("bt_person", str(person_id))
    body = c.get("/b/test-secret/me").text
    assert 'value="3"' in body
    assert "2026-08-12" in body


def test_сохранение_настроек():
    person_id = app_module.add_person("Егор")
    c = client()
    c.cookies.set("bt_person", str(person_id))
    response = c.post(
        "/b/test-secret/me",
        data={"cycle_on": "2", "cycle_off": "2", "anchor": "2026-08-12", "mode": ALTERNATE},
        follow_redirects=False,
    )
    assert response.status_code == 303
    person = app_module.list_people()[0]
    assert person.mode == ALTERNATE
    assert person.anchor == date(2026, 8, 12)


def test_некорректный_цикл_отклоняется():
    person_id = app_module.add_person("Егор")
    c = client()
    c.cookies.set("bt_person", str(person_id))
    response = c.post(
        "/b/test-secret/me",
        data={"cycle_on": "0", "cycle_off": "2", "anchor": "2026-08-12", "mode": DAY},
    )
    assert response.status_code == 400
    assert app_module.list_people()[0].cycle_on == 2  # не сохранилось
```

- [ ] **Step 2: Запустить тест, убедиться что падает**

Run: `.venv/bin/pytest test_app.py -q`
Expected: FAIL — маршрута `/me` нет, ответ 404.

- [ ] **Step 3: Создать `templates/me.html`**

```html
{% extends "base.html" %}
{% block content %}
<h1>График {{ person.name }}</h1>
<form method="post" action="/b/{{ secret }}/me">
  <label>Смен подряд <input type="number" name="cycle_on" min="1" max="14" value="{{ person.cycle_on }}"></label>
  <label>Выходных подряд <input type="number" name="cycle_off" min="1" max="14" value="{{ person.cycle_off }}"></label>
  <label>Дата первой смены цикла <input type="date" name="anchor" value="{{ person.anchor.isoformat() }}"></label>
  <label>Режим
    <select name="mode">
      <option value="day" {% if person.mode == "day" %}selected{% endif %}>всегда дневные</option>
      <option value="night" {% if person.mode == "night" %}selected{% endif %}>всегда ночные</option>
      <option value="alternate" {% if person.mode == "alternate" %}selected{% endif %}>чередую день/ночь</option>
    </select>
  </label>
  <button type="submit">Сохранить</button>
</form>
<p class="hint">Отдельные дни — подмены, отпуск, свои дела — правятся тыком в матрице на главной.</p>
{% endblock %}
```

- [ ] **Step 4: Написать маршруты**

Дописать в `app.py`:

```python
@app.get("/b/{secret}/me")
def me_form(request: Request, secret: str):
    check_secret(secret)
    person_id = me_id(request)
    if person_id is None:
        return RedirectResponse(f"/b/{secret}/join", status_code=303)
    person = next(p for p in list_people() if p.id == person_id)
    return templates.TemplateResponse(request, "me.html", {"secret": secret, "person": person})


@app.post("/b/{secret}/me")
def me_save(
    request: Request,
    secret: str,
    cycle_on: int = Form(...),
    cycle_off: int = Form(...),
    anchor: str = Form(...),
    mode: str = Form(...),
):
    check_secret(secret)
    person_id = me_id(request)
    if person_id is None:
        raise HTTPException(status_code=403)
    if cycle_on < 1 or cycle_off < 1 or mode not in (DAY, NIGHT, ALTERNATE):
        raise HTTPException(status_code=400, detail="Некорректный график")
    update_person(person_id, cycle_on, cycle_off, date.fromisoformat(anchor), mode)
    return RedirectResponse(f"/b/{secret}/", status_code=303)
```

Импорт `NIGHT` из `schedule` добавить, если его ещё нет.

- [ ] **Step 5: Запустить тесты, убедиться что проходят**

Run: `.venv/bin/pytest -q`
Expected: PASS, 38 тестов.

- [ ] **Step 6: Коммит**

```bash
git add app.py templates/me.html test_app.py
git commit -m "feat: страница настроек сменного цикла"
```

---

### Task 10: Развёртывание на VPS

**Files:**
- Create: `deploy/beertime.service`, `deploy/nginx.conf`
- Modify: `README.md`

**Interfaces:**
- Consumes: `app:app`, переменные окружения `SECRET`, `TZ`, `DB_PATH`
- Produces: инструкция и файлы, которых достаточно для запуска на чистом сервере

- [ ] **Step 1: Создать `deploy/beertime.service`**

```ini
[Unit]
Description=BeerTime
After=network.target

[Service]
User=beertime
WorkingDirectory=/opt/beertime
EnvironmentFile=/opt/beertime/.env
ExecStart=/opt/beertime/.venv/bin/uvicorn app:app --host 127.0.0.1 --port 8021
Restart=always

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 2: Создать `deploy/nginx.conf`**

```nginx
server {
    listen 443 ssl;
    server_name beertime.example.com;

    ssl_certificate     /etc/letsencrypt/live/beertime.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/beertime.example.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8021;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name beertime.example.com;
    return 301 https://$host$request_uri;
}
```

- [ ] **Step 3: Дописать раздел развёртывания в `README.md`**

```markdown
## Развёртывание на VPS

    sudo useradd -r -m -d /opt/beertime beertime
    sudo -u beertime git clone <repo> /opt/beertime
    cd /opt/beertime
    sudo -u beertime python -m venv .venv
    sudo -u beertime .venv/bin/pip install -r requirements.txt
    sudo -u beertime cp .env.example .env   # вписать настоящий SECRET

    sudo cp deploy/beertime.service /etc/systemd/system/
    sudo systemctl enable --now beertime

    sudo cp deploy/nginx.conf /etc/nginx/sites-available/beertime
    sudo ln -s /etc/nginx/sites-available/beertime /etc/nginx/sites-enabled/
    sudo nginx -t && sudo systemctl reload nginx

Бэкап базы: `sqlite3 /opt/beertime/beertime.db ".backup /var/backups/beertime-$(date +%F).db"`.

Обновление: `git pull && sudo systemctl restart beertime`.
```

- [ ] **Step 4: Проверить, что приложение поднимается локально**

Run: `SECRET=local-test .venv/bin/uvicorn app:app --port 8021 &` затем `sleep 2 && curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8021/b/local-test/join` и остановить процесс
Expected: `200`

- [ ] **Step 5: Полный прогон тестов**

Run: `.venv/bin/pytest -q`
Expected: PASS, 38 тестов.

- [ ] **Step 6: Коммит**

```bash
git add deploy README.md
git commit -m "chore: systemd, nginx и инструкция по развёртыванию"
```
